#Create vector that contains numbers 
s=c(10,20,30,40,50,60)
#find the mean of the vector
print(mean(s))
#computes the sample variance of the vector
print(var(s))

#Create vector that contains numbers  
g=c(25,30,35,35,40,45)
#find the mean of the vector
print(mean(g))
#computes the sample variance of the vector
print(var(g))

